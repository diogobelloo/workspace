// O método some() testa se ao menos um dos elementos no array passa na condição e retona um valor true ou false.

// Exemplo ded array de idades 

const ages = [ 15, 30, 39, 29]

const result = ages.some(age => age < 18)

console.log(result)